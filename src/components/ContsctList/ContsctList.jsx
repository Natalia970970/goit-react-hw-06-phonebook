export const ContactList = ({contacts}) => {
    return (
        <ul>
            {/* {contacts.map(({id, contact}) => {
            return (
                <li key={id}>
                    {contact}
                </li>
            );
        })} */}
        </ul>
    );
};